const
    { spawn } = require('child_process'),
    package   = '@bryntum/gantt-trial-lib',
    script    = `${package}/build.js`;
try {
    spawn('node', [require.resolve(script)], { cwd : process.cwd(), stdio : 'inherit' });
}
catch (e) {
    console.log(`Optional ${package} is not installed`);
}